{
	"name": "ts-simple-example",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "esr index.ts",
		"start": "node dist/index.js",
		"build": "esbuild index.ts --bundle --platform=node --outfile=dist/index.js"
	},
	"devDependencies": {
		"esbuild": "^0.14.50",
		"esbuild-runner": "^2.2.1"
	},
	"dependencies": {
	}
}
